---
name: test-flow
description: Triggers on /test-flow only. Alias for /flutter-e2e's plan-file mode - drives a Flutter web app via Playwright against a test plan markdown file, marking each step pass/fail/skip inline.
---

# /test-flow

> Alias. All content lives in `/flutter-e2e` (Mode B - Plan-file) so the two can never drift.

Read `~/.claude/skills/flutter-e2e/SKILL.md` in full and follow its plan-file
mode exactly, passing `$ARGUMENTS` through as-is. Do not reimplement or
summarize any of its steps here - this file is a pointer, nothing else.
